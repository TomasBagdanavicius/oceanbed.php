<?php

declare(strict_types=1);

namespace LWP\Components\Rules;

interface FormatterInterface
{
    /* Exists to primarily create a class type. The `format()` method is complicated, because parameter and return types heavily vary. */

}
